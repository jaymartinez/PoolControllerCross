<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:PoolControllerCross.ViewModels"
             x:Class="PoolControllerCross.Views.PoolAndSpaPage">
  <!--Pages can be added as references or inline-->

    <ContentPage Title="{Binding PoolModel.Title}">
        <StackLayout Orientation="Vertical" Padding="20" Spacing="20">
            <ActivityIndicator x:Name="PoolPumpActivityIndicator" IsRunning="{Binding IsLoading}"/>
            <StackLayout Orientation="Horizontal" >
                <Label Text="Status: " FontSize="Small" VerticalOptions="Center" />
                <Label x:Name="ActiveAtLabel" Text="{Binding PoolModel.ActiveAtText}" TextColor="Green" FontSize="Small" VerticalOptions="Center" />
                <Switch Margin="40,0,0,0" IsToggled="{Binding PoolModel.PoolIsActive}" />
            </StackLayout>

            <StackLayout Orientation="Horizontal" Margin="0,10,0,10">
                <CheckBox IsChecked="{Binding PoolModel.ScheduleIsActive}" />
                <Label Text="Enable/Disable Timer" FontSize="Small" VerticalOptions="Center" />
                <CheckBox Margin="50,0,0,0" IsChecked="{Binding PoolModel.IncludeBooster}" />
                <Label Text="Run Booster Pump With Schedule" FontSize="Small" VerticalOptions="Center" />
            </StackLayout>

            <StackLayout Orientation="Horizontal" Margin="0,10,0,10">
                <TimePicker Margin="20" Time="{Binding PoolModel.StartTime}" />
                <TimePicker Margin="20" Time="{Binding PoolModel.EndTime}" />
            </StackLayout>

        </StackLayout>
    </ContentPage>

  <ContentPage Title="Spa Pump" />
  <ContentPage Title="Booster Pump" />
  <ContentPage Title="Heater" />
</TabbedPage>